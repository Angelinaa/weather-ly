<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="title_update_progressbar_style">
        <item name="android:maxWidth">45dp</item>
        <item name="android:maxHeight">45dp</item>
        <!--<item name="android:indeterminateDrawable">@drawable/animated_rotate</item> -->
        <item name="android:minWidth">45dp</item>
        <item name="android:minHeight">45dp</item>
    </style>

</resources>
